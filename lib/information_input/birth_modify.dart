import 'package:flutter/material.dart';

class BirthModify extends StatelessWidget {
  const BirthModify({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('우리 아이 생일 바꾸기')),
      body: Container(),
    );
  }
}
